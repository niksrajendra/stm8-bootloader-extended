#include "mainwindow.h"

MainWindow::MainWindow(QWidget *parent)
    : QMainWindow(parent)
{
    // Create the parent window widget
    //QWidget *window = new QWidget();


}

MainWindow::~MainWindow() = default;
